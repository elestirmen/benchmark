# Kod kökeni

`goruntu_islemleri.py`, bağımsız benchmark deposu oluşturulurken aşağıdaki
çalışma alanı dosyasından başlangıç kopyası olarak alınmıştır:

```text
C:\d_surucusu\visual_navigation\autoencoder_sat_to_map\src\goruntu_islemleri.py
```

Başlangıç kopyasının SHA-256 değeri:

```text
C17ECFBA2C47499E26A1E788CE1C8040B160C36A59E678D13C438FB73DA6F814
```

Bu depo kopyası daha sonra benchmark gereksinimleri için değiştirilmiştir:
test edilebilir model-output dönüşümü, tek-yüklemeli `LoadedModelRuntime`, GPU
fail-fast ve `.hdf5` taraması eklenmiştir. Yukarıdaki hash güncel dosyanın değil,
yalnız köken alınan başlangıç kopyasının kimliğidir.

Özgün kaynak dosya silinmemiş, taşınmamış veya yeniden adlandırılmamıştır. Yerel
`models` klasöründeki çalışma zamanı ağırlıkları da
`template-matching/top_modeller` dizininden kopyalanmış ve Git kapsamı dışında
bırakılmıştır.
